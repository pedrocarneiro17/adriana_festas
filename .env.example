# Desenvolvimento local: SQLite, não precisa mudar nada (usado por prisma/local/schema.prisma).
# Produção (Railway): sobrescreva com a connection string do Postgres, ex.
# "postgresql://usuario:senha@host:5432/adriana_festas?schema=public" (usado por prisma/schema.prisma).
DATABASE_URL="file:./dev.db"
AUTH_SECRET="troque-para-uma-string-aleatoria-e-longa-em-producao"
